import styled from '@emotion/styled';
import React from 'react';

const StepBarBox = styled.div``;

function StepBar(): JSX.Element {
	return <StepBarBox></StepBarBox>;
}

export default React.memo(StepBar);
